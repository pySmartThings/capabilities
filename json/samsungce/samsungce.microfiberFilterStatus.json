{
  "id": "samsungce.microfiberFilterStatus",
  "version": 1,
  "status": "proposed",
  "name": "Microfiber filter status",
  "ephemeral": false,
  "attributes": {
    "supportedStatus": {
      "schema": {
        "title": "Supported Status",
        "type": "object",
        "properties": {
          "value": {
            "type": "array",
            "items": { "type": "string", "enum": ["normal", "blockage"] }
          }
        },
        "additionalProperties": false,
        "required": ["value"]
      },
      "enumCommands": []
    },
    "status": {
      "schema": {
        "title": "Status of Microfiber Filter",
        "type": "object",
        "properties": {
          "value": { "type": "string", "enum": ["normal", "blockage"] }
        },
        "additionalProperties": false,
        "required": ["value"]
      },
      "enumCommands": []
    }
  },
  "commands": {}
}
