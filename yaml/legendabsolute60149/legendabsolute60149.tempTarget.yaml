id: legendabsolute60149.tempTarget
version: 1
status: proposed
name: Temp Target
ephemeral: false
attributes:
  tempTarget:
    schema:
      type: object
      properties:
        value:
          type: string
      additionalProperties: false
      required:
        - value
    setter: setTempTarget
    enumCommands: []
commands:
  setTempTarget:
    name: setTempTarget
    arguments:
      - name: value
        optional: false
        schema:
          type: string
