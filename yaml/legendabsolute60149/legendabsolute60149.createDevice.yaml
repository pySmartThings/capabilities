id: legendabsolute60149.createDevice
version: 1
status: proposed
name: Create Device
ephemeral: false
attributes:
  createDevice:
    schema:
      type: object
      properties:
        value:
          type: string
      additionalProperties: false
      required:
        - value
    setter: setCreateDevice
    enumCommands: []
commands:
  setCreateDevice:
    name: setCreateDevice
    arguments:
      - name: value
        optional: false
        schema:
          type: string
    sensitive: false
